{
  "scripts": {
    "index": "hugo --minify && npx pagefind --site \"public\" --output-subdir ../static/pagefind --force-language en",
    "start": "npm run index && hugo server"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "modern-normalize": "^3.0.0",
    "pagefind": "^1.0.4",
    "postcss": "^8.4.35",
    "postcss-cli": "^11.0.0"
  }
}
