{
  "scripts": {
    "index": "hugo --minify --gc && npx pagefind --site \"public\" --output-subdir ../static/pagefind --force-language en",
    "start": "npm run index && hugo server"
  },
  "devDependencies": {
    "modern-normalize": "^3.0.0",
    "pagefind": "^1.0.4",
    "postcss": "^8.5.8",
    "postcss-cli": "^11.0.1"
  },
  "dependencies": {
    "postcss-preset-env": "^11.2.0"
  }
}
